# 快速上手｜AI组织官

## 1. 下载与解压

Windows 用户建议解压到较短路径，例如 `C:\DFMH\DFMH-ZuZhiGuan`，避免系统长路径限制。

## 2. 本地验收

安装 Node.js 20 或更高版本，在仓库根目录执行：

```powershell
npm test
```

看到 `PASS` 后再开始任务。

## 3. 在 AI 工具中使用

把本仓库作为独立项目打开，先让 AI 读取根目录 `AGENTS.md`、`PUBLIC_PACKAGE_CONTRACT.json` 和主入口文件，再发送 `examples/REQUEST.md` 中的任务格式。

## 4. 外部条件

- Node.js 20 或更高版本，以及能够读取本地项目文件的 AI 工具。
- 使用者提供的真实企业、项目和任务资料；本仓库不附带任何客户数据。
- 需要最新市场、法规、平台或竞品事实时，由使用者环境提供可访问的网络检索能力。

## 5. 能力边界

本仓库提供单个组织或单项技能的公开执行规则，不等于完整AI数字员工控制中心。若需要飞书调度、五组织协作、跨项目资产绑定或私有知识库，需要另行部署完整控制中心。
